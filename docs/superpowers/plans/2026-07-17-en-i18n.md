# EN-версия на /en/ — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Английская версия сайта становится индексируемой: `/en/` и `/en/life/` как отдельные URL, язык выводится из URL, hreflang-пары взаимные.

**Architecture:** «Тонкие головы» — два новых физических HTML-файла со статическими EN-головами, тело рендерит существующий JS из `CONTENT.en`. Язык — производная `location.pathname`, сетка URL живёт в одной функции `pageURL(page, lang)`. Все инварианты четырёх голов держит таблица `PAGES` в test.js.

**Tech Stack:** vanilla JS, без сборки; node test.js; GitHub Pages.

**Спека:** `docs/superpowers/specs/2026-07-17-en-i18n-design.md` — вердикты консилиума там, план им следует.

## Global Constraints

- **НИКОГДА не коммитить без явного разрешения Макса** (CLAUDE.md). Каждый commit-шаг = стоп: показать дифф, ждать слов «коммить»/«можно коммитить»/«ок, коммит». «Отлично»/«погнали» — НЕ разрешение.
- Рабочая директория — worktree `/Users/artfaal/PROJECTS/PERSONAL_PAGE/.claude/worktrees/en-i18n`, ветка `en-i18n`. Не выходить.
- `content.js` НЕ трогаем — весь EN-контент уже там. `meta.last_updated` НЕ трогаем (контент не меняется).
- В тестах никаких хардкодов значений — всё деривируется из `PAGES` и `CONTENT`.
- Тесты: `SKIP_PDF_GEN=1 node test.js` — зелёные после каждой задачи.
- Проверка в браузере до коммита — обновить `?v=` вручную блоком из `.githooks/pre-commit` (иначе кэш).
- URL строго строчные, с trailing slash, абсолютные в head.

---

### Task 1: test.js — таблица-объекты, asserts из единого источника (коммит 1)

Рефактор на **текущих двух** страницах. Поведение сайта не меняется, тест зелёный до и после. Src-файлы не трогаются.

**Files:**
- Modify: `test.js:249` (PAGES), `test.js:307-346` (HTML-цикл), `test.js:369` (sitemap), `test.js:398-416` (nav), `test.js:420-437` (updateMeta)

**Interfaces:**
- Produces: `PAGES = {файл: {url, lang, page}}`, `altOf(url)` — Task 2 добавляет записи и новые asserts поверх этой структуры.

- [ ] **Step 1: PAGES → объекты + altOf** (замена test.js:249 и соседних строк)

```js
// Единый источник путей страниц. Меняешь URL-структуру — правишь только здесь.
// Стаб-редиректы сюда не входят: у них нет ни JSON-LD, ни og — им отдельный assert.
// page — ключ рендера ('cv'/'personal'), совпадает с currentPage в initPage().
const HOST = 'https://' + fs.readFileSync(path.join(ROOT, 'CNAME'), 'utf8').trim();
const PAGES = {
  'index.html':      { url: '/',      lang: 'ru', page: 'cv'       },
  'life/index.html': { url: '/life/', lang: 'ru', page: 'personal' },
};
// Парный URL другого языка — вычисляется, не хранится: нельзя опечатать то, чего нет.
const altOf = u => u.startsWith('/en') ? (u.slice(3) || '/') : '/en' + u;
Object.values(PAGES).forEach(p => {
  assert('altOf round-trip: ' + p.url, altOf(altOf(p.url)) === p.url, altOf(altOf(p.url)));
});
```

- [ ] **Step 2: HTML-цикл — всё из таблицы** (замена test.js:307-346 целиком)

```js
Object.entries(PAGES).forEach(([file, p]) => {
  const html = fs.readFileSync(path.join(ROOT, file), 'utf8');
  const L = CONTENT[p.lang];
  assert(file + ' <html lang="' + p.lang + '">', html.includes('<html lang="' + p.lang + '"'));
  assert(file + ' has meta description', html.includes('meta name="description"'));
  assert(file + ' has JSON-LD', html.includes('application/ld+json'));
  assert(file + ' has favicon link', /rel=["']icon["']/.test(html));

  // Дрифт-ловушка: обе страницы с canonical на "/" → Google молча выкидывает вторую.
  // Ни 404, ни ошибки в консоли, ни визуальной разницы — поэтому только тест это и поймает.
  const want = HOST + p.url;
  const grab = re => (html.match(re) || [])[1];
  assert(file + ' canonical === ' + want, grab(/rel="canonical"\s+href="([^"]+)"/) === want);
  assert(file + ' og:url === ' + want, grab(/property="og:url"\s+content="([^"]+)"/) === want);
  assert(file + ' hreflang self === ' + want,
    grab(new RegExp('hreflang="' + p.lang + '"\\s+href="([^"]+)"')) === want);

  // og заполнен статикой: краулеры соцсетей JS не исполняют — без статики карточка пустая.
  // Привязываем к content.js, иначе разъедется при первой же правке контента.
  // og:description намеренно без стажа: в JS он считается из meta.start_it, хардкод бы протух.
  const wantOgTitle = p.page === 'cv' ? L.meta.title_cv : L.meta.title_personal;
  const wantOgDesc = L.hero.role + '. ' + L.hero.tagline;
  assert(file + ' og:title === content.js', grab(/property="og:title"\s+content="([^"]+)"/) === wantOgTitle,
    'в HTML: ' + grab(/property="og:title"\s+content="([^"]+)"/) + ' | в content.js: ' + wantOgTitle);
  assert(file + ' og:description === content.js', grab(/property="og:description"\s+content="([^"]+)"/) === wantOgDesc);
  // Превью — аватар, не фото: фото открывается по клику и печатается в PDF.
  assert(file + ' og:image === аватар', grab(/property="og:image"\s+content="([^"]+)"/) === HOST + '/assets/avatar.webp');
  assert(file + ' og:site_name', grab(/property="og:site_name"\s+content="([^"]+)"/) === L.meta.host);
  assert(file + ' og:locale', grab(/property="og:locale"\s+content="([^"]+)"/) === (p.lang === 'en' ? 'en_US' : 'ru_RU'));
  assert(file + ' og:type', grab(/property="og:type"\s+content="([^"]+)"/) === (p.page === 'cv' ? 'profile' : 'website'));

  // Cache-busting + существование: (?:\.\.\/)* — ЛЮБАЯ глубина (старый regex с ? был слеп
  // на глубине 2: matcher пустой → цикл ноль раз → тест зелёный при битой странице).
  const dir = path.dirname(path.join(ROOT, file));
  const localAssetRe = /(?:src|href)=["']((?:\.\.\/)*src\/(?:js|styles)\/[a-zA-Z0-9._-]+)/g;
  const refs = [...html.matchAll(localAssetRe)].map(m => m[1]);
  assert(file + ' ссылается на src-ассеты', refs.length > 0, 'regex не нашёл ни одного — сломан?');
  refs.forEach(ref => {
    assert(file + ' asset exists: ' + ref, fs.existsSync(path.resolve(dir, ref)), 'битая глубина ../?');
    const hashRe = new RegExp(ref.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '\\?v=[a-f0-9]{6,12}');
    assert(file + ' cache-bust: ' + ref.split('/').pop(), hashRe.test(html), 'no ?v=<hash> suffix');
  });
});
```

(Строка `assert(file + ' has og:title'...)` и `' has canonical'` из старого цикла поглощены точными grab-asserts.)

- [ ] **Step 3: sitemap-сверка на .url** (test.js:369)

```js
const wantLocs = Object.values(PAGES).map(p => HOST + p.url).sort();
```

- [ ] **Step 4: nav-тест — скоуп по role="navigation"** (внутри блока test.js:398-416, замена tabsOf)

```js
// Только вкладки страниц: парсим контейнер role="navigation", lang-группа (role="group") отдельно.
const tabsOf = page => {
  const el = {};
  sandbox.renderNav(el, c, page);
  const seg = (el.innerHTML.match(/<div class="seg" role="navigation">([\s\S]*?)<\/div>/) || [])[1] || '';
  return [...seg.matchAll(/<a href="([^"]*)" class="seg-btn([^"]*)"[^>]*>([^<]*)<\/a>/g)]
    .map(m => ({ href: m[1], on: m[2].includes('is-on'), text: m[3] }));
};
```

Существующие asserts (`работа первая`, `подсвечена работа/жизнь`) не меняются.

- [ ] **Step 5: updateMeta-прогон по таблице** (замена блока test.js:420-437)

```js
// updateMeta перезаписывает og на лету. Если разъедется со статикой в <head> —
// краулер соцсети покажет одно, браузер (и Google, который рендерит JS) другое,
// и оба будут «работать». Сверяем по каждой странице из таблицы.
{
  Object.entries(PAGES).forEach(([file, p]) => {
    const set = {};
    sandbox.document = {
      documentElement: {}, title: '',
      querySelector: sel => ({ setAttribute: (_k, v) => { set[sel] = v; } }),
      getElementById: () => ({ textContent: '' }),
    };
    sandbox.location = { pathname: p.url };
    vm.runInContext('_lang = ' + JSON.stringify(p.lang), sandbox);
    sandbox.updateMeta(CONTENT[p.lang], p.page);

    const html = fs.readFileSync(path.join(ROOT, file), 'utf8');
    const stat = re => (html.match(re) || [])[1];
    assert(file + ' updateMeta og:image === статика', set['meta[property="og:image"]'] === stat(/property="og:image"\s+content="([^"]+)"/),
      'JS: ' + set['meta[property="og:image"]'] + ' | HTML: ' + stat(/property="og:image"\s+content="([^"]+)"/));
    assert(file + ' updateMeta og:title === статика', set['meta[property="og:title"]'] === stat(/property="og:title"\s+content="([^"]+)"/));
    assert(file + ' updateMeta og:locale === статика', set['meta[property="og:locale"]'] === stat(/property="og:locale"\s+content="([^"]+)"/));
    // og:description НЕ сверяем: на CV рантайм считает стаж из дат, статика намеренно без стажа.
  });
  vm.runInContext('_lang = "ru"', sandbox); // вернуть дефолт для последующих блоков
}
```

- [ ] **Step 6: прогнать тесты**

Run: `SKIP_PDF_GEN=1 node test.js`
Expected: `Result: N passed, 0 failed` (N вырастет — новых asserts больше, чем поглощённых).

- [ ] **Step 7: СТОП — показать дифф Максу, ждать явного ОК на коммит**

```bash
git add test.js
git commit -m "refactor: таблица страниц test.js — объекты, asserts из единого источника

PAGES теперь несёт {url, lang, page}, парный URL вычисляется altOf().
Все per-page проверки (canonical, og, hreflang, locale, type) деривируются
из таблицы и content.js — добавление страницы не требует новых хардкодов.
Починено слепое пятно: regex кэш-бастинга не видел глубину ../../ (цикл
проверок молча не выполнялся); добавлена проверка существования ассетов
по фактической глубине файла. Nav-тест заскоуплен контейнером
role=\"navigation\". updateMeta сверяется со статикой каждой страницы."
```

---

### Task 2: EN-версия — /en/ и /en/life/ (коммит 2)

**Files:**
- Create: `en/index.html`, `en/life/index.html`
- Modify: `src/js/shared.js:71-77` (getLang/setLang→pageURL), `src/js/shared.js:118-141` (renderNav), `index.html:18-20` (og:locale:alternate + hreflang), `life/index.html:18-20` (то же), `sitemap.xml`, `test.js` (записи PAGES + новые asserts), `CLAUDE.md` (структура + правило _lang), `README.md` (раздел EN)

**Interfaces:**
- Consumes: `PAGES`/`altOf` из Task 1.
- Produces: `getLang(): 'ru'|'en'` из pathname; `pageURL(page: 'cv'|'personal', lang: 'ru'|'en'): string` — сетка `/`, `/life/`, `/en/`, `/en/life/`.

- [ ] **Step 1: shared.js — getLang из URL, pageURL, выпил setLang** (замена :70-77)

```js
// ── Язык ── (язык = URL: /en/* — английский, остальное — русский; localStorage не участвует)
function getLang() {
  return /^\/en(\/|$)/.test(location.pathname) ? 'en' : 'ru';
}

// Сетка URL «страница × язык» — единственный владелец всех внутренних ссылок.
function pageURL(page, lang) {
  return (lang === 'en' ? '/en/' : '/') + (page === 'personal' ? 'life/' : '');
}
```

`setLang` удаляется целиком (иначе dead-code-тест JS-функций упадёт — и правильно).

- [ ] **Step 2: shared.js — renderNav целиком** (замена :118-141, с учётом сдвига после Step 1)

```js
// ── Рендер: Навигация ──
function renderNav(el, c, currentPage) {
  const lang = _lang;
  const isPersonal = currentPage === 'personal';
  el.innerHTML = ''
    + `<a class="tb-brand" href="${pageURL('cv', lang)}">`
    +   `<span class="tb-dot"></span>`
    +   `<span class="tb-name">${escapeHTML(c.meta.host)}</span>`
    + `</a>`
    + `<div class="tb-right">`
    +   `<div class="seg" role="navigation">`
    +     `<span class="seg-label">--page=</span>`
    +     `<a href="${pageURL('cv', lang)}" class="seg-btn ${!isPersonal ? 'is-on' : ''}"${!isPersonal ? ' aria-current="page"' : ''}>${escapeHTML(c.nav.cv)}</a>`
    +     `<span class="seg-sep">|</span>`
    +     `<a href="${pageURL('personal', lang)}" class="seg-btn ${isPersonal ? 'is-on' : ''}"${isPersonal ? ' aria-current="page"' : ''}>${escapeHTML(c.nav.personal)}</a>`
    +   `</div>`
    +   `<div class="seg" role="group" aria-label="lang">`
    +     `<span class="seg-label">--lang=</span>`
    +     `<a href="${pageURL(currentPage, 'ru')}" hreflang="ru" class="seg-btn ${lang === 'ru' ? 'is-on' : ''}"${lang === 'ru' ? ' aria-current="page"' : ''}>ru</a>`
    +     `<span class="seg-sep">|</span>`
    +     `<a href="${pageURL(currentPage, 'en')}" hreflang="en" class="seg-btn ${lang === 'en' ? 'is-on' : ''}"${lang === 'en' ? ' aria-current="page"' : ''}>en</a>`
    +   `</div>`
    +   `<a href="${cvPdfHref()}" download class="seg-cv">${icon('download', 12)} PDF</a>`
    + `</div>`;
}
```

Изменения относительно текущего: бренд и вкладки — через `pageURL` (внутри EN ведут на EN), языковой переключатель — `<a href>` вместо `<button onclick>`, `aria-current="page"` на активных. PDF-строка не тронута — после rebase master она уже ходит через `cvPdfHref()` (хелпер Макса, читает `_lang`), URL-язык он переживает без правок.

Актуализация после rebase (2026-07-17): RU `title_cv` теперь «Соловьев Максим — резюме DevOps-инженера» — asserts берут его из content.js, правок плана не требует; EN-строки не менялись. `?v=`-хэши в EN-головах копировать из АКТУАЛЬНЫХ RU-файлов, не из примеров плана.

- [ ] **Step 3: RU-головы — hreflang-блок и og:locale:alternate**

`index.html`: строку 18 (`og:locale`) дополнить, строку 20 (одиночный hreflang) заменить блоком:
```html
  <meta property="og:locale" content="ru_RU">
  <meta property="og:locale:alternate" content="en_US">
  <meta property="og:site_name" content="artfaal.ru">
  <link rel="alternate" hreflang="ru" href="https://artfaal.ru/">
  <link rel="alternate" hreflang="en" href="https://artfaal.ru/en/">
  <link rel="alternate" hreflang="x-default" href="https://artfaal.ru/">
```

`life/index.html`: аналогично, URL пары `/life/` и `/en/life/`:
```html
  <meta property="og:locale" content="ru_RU">
  <meta property="og:locale:alternate" content="en_US">
  <meta property="og:site_name" content="artfaal.ru">
  <link rel="alternate" hreflang="ru" href="https://artfaal.ru/life/">
  <link rel="alternate" hreflang="en" href="https://artfaal.ru/en/life/">
  <link rel="alternate" hreflang="x-default" href="https://artfaal.ru/life/">
```

- [ ] **Step 4: en/index.html — полный файл**

Копия `index.html` c дельтой: `lang`, title/description/og из `CONTENT.en`, свои canonical/og:url, `en_US`, hreflang-блок CV-пары, префиксы `src/` → `../src/`. `?v=`-хэши — текущие из `index.html` (хук перештампует при коммите).

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Maksim Solovev — DevOps CV</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">

  <!-- SEO (generic fallback — JS обновит из content.js) -->
  <meta name="description" content="Maksim Solovev — DevOps Expert Engineer.">
  <meta name="author" content="Максим Соловьев">
  <link rel="canonical" href="https://artfaal.ru/en/">
  <meta property="og:type" content="profile">
  <meta property="og:title" content="Maksim Solovev — DevOps CV">
  <meta property="og:description" content="DevOps Engineer. I bring order to complex systems and automate the grind so people have energy left for life.">
  <meta property="og:url" content="https://artfaal.ru/en/">
  <meta property="og:image" content="https://artfaal.ru/assets/avatar.webp">
  <meta property="og:locale" content="en_US">
  <meta property="og:locale:alternate" content="ru_RU">
  <meta property="og:site_name" content="artfaal.ru">
  <link rel="alternate" hreflang="ru" href="https://artfaal.ru/">
  <link rel="alternate" hreflang="en" href="https://artfaal.ru/en/">
  <link rel="alternate" hreflang="x-default" href="https://artfaal.ru/">
  <script type="application/ld+json" id="jsonld">{}</script>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@700&family=Inter+Tight:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

  <!-- Styles -->
  <link rel="stylesheet" href="../src/styles/base.css?v=de85e3fd">
  <link rel="stylesheet" href="../src/styles/layout.css?v=7710a623">
  <link rel="stylesheet" href="../src/styles/components.css?v=70e8d5da">
</head>
<body>
  <div class="page-wrap">
    <nav class="topbar" id="topbar"></nav>
    <article>
      <header class="hero" id="hero"></header>
      <main id="main"></main>
    </article>
    <footer class="foot" id="footer"></footer>
  </div>

  <script src="../src/js/utils.js?v=cb27566f"></script>
  <script src="../src/js/content.js?v=fcdfb655"></script>
  <script src="../src/js/icons.js?v=7d327c58"></script>
  <script src="../src/js/shared.js?v=1495846b"></script>
  <script src="../src/js/page-cv.js?v=3892f25d"></script>
</body>
</html>
```

**og:title/og:description не сочинять** — это `CONTENT.en.meta.title_cv` и `CONTENT.en.hero.role + '. ' + CONTENT.en.hero.tagline` дословно (тест сверит; при расхождении — правда в content.js).

- [ ] **Step 5: en/life/index.html — полный файл**

Копия `life/index.html` (шрифт Caveat едет с ним!) с дельтой: префиксы `../src/` → `../../src/`.

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Maksim Solovev — DevOps</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">

  <!-- SEO (generic fallback — JS обновит из content.js) -->
  <meta name="description" content="Maksim Solovev — DevOps Engineer.">
  <meta name="author" content="Максим Соловьев">
  <link rel="canonical" href="https://artfaal.ru/en/life/">
  <meta property="og:type" content="website">
  <meta property="og:title" content="Maksim Solovev — DevOps">
  <meta property="og:description" content="DevOps Engineer. I bring order to complex systems and automate the grind so people have energy left for life.">
  <meta property="og:url" content="https://artfaal.ru/en/life/">
  <meta property="og:image" content="https://artfaal.ru/assets/avatar.webp">
  <meta property="og:locale" content="en_US">
  <meta property="og:locale:alternate" content="ru_RU">
  <meta property="og:site_name" content="artfaal.ru">
  <link rel="alternate" hreflang="ru" href="https://artfaal.ru/life/">
  <link rel="alternate" hreflang="en" href="https://artfaal.ru/en/life/">
  <link rel="alternate" hreflang="x-default" href="https://artfaal.ru/life/">
  <script type="application/ld+json" id="jsonld">{}</script>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@500;600&family=IBM+Plex+Mono:wght@700&family=Inter+Tight:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

  <!-- Styles -->
  <link rel="stylesheet" href="../../src/styles/base.css?v=de85e3fd">
  <link rel="stylesheet" href="../../src/styles/layout.css?v=7710a623">
  <link rel="stylesheet" href="../../src/styles/components.css?v=70e8d5da">
</head>
<body>
  <div class="page-wrap">
    <nav class="topbar" id="topbar"></nav>
    <article>
      <header class="hero" id="hero"></header>
      <main id="main"></main>
    </article>
    <footer class="foot" id="footer"></footer>
  </div>

  <script src="../../src/js/utils.js?v=cb27566f"></script>
  <script src="../../src/js/content.js?v=fcdfb655"></script>
  <script src="../../src/js/icons.js?v=7d327c58"></script>
  <script src="../../src/js/shared.js?v=1495846b"></script>
  <script src="../../src/js/page-personal.js?v=a75e7630"></script>
</body>
</html>
```

- [ ] **Step 6: sitemap.xml — 4 `<loc>`**

После блока `/life/` добавить (lastmod — хук перепишет на дату коммита):
```xml
  <url>
    <loc>https://artfaal.ru/en/</loc>
    <lastmod>2026-07-17</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://artfaal.ru/en/life/</loc>
    <lastmod>2026-07-17</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
  </url>
```

- [ ] **Step 7: test.js — записи PAGES, зеркальность пар, hreflang-пары, шрифты**

PAGES:
```js
const PAGES = {
  'index.html':         { url: '/',         lang: 'ru', page: 'cv'       },
  'life/index.html':    { url: '/life/',    lang: 'ru', page: 'personal' },
  'en/index.html':      { url: '/en/',      lang: 'en', page: 'cv'       },
  'en/life/index.html': { url: '/en/life/', lang: 'en', page: 'personal' },
};
```

Рядом с altOf round-trip — зеркальность пар:
```js
const byUrl = Object.fromEntries(Object.values(PAGES).map(p => [p.url, p]));
Object.values(PAGES).forEach(p => {
  const a = byUrl[altOf(p.url)];
  assert('языковая пара для ' + p.url, !!a && a.lang !== p.lang && a.page === p.page,
    'нет зеркальной записи ' + altOf(p.url));
});
```

В HTML-цикле заменить одиночный `hreflang self` assert на пары (взаимность гарантирована тем, что оба конца считаются из одной таблицы):
```js
  const pairRu = HOST + (p.lang === 'ru' ? p.url : altOf(p.url));
  const pairEn = HOST + (p.lang === 'en' ? p.url : altOf(p.url));
  assert(file + ' hreflang ru', grab(/hreflang="ru"\s+href="([^"]+)"/) === pairRu);
  assert(file + ' hreflang en', grab(/hreflang="en"\s+href="([^"]+)"/) === pairEn);
  // x-default → RU-член кластера (вердикт консилиума: одна шляпа с hreflang ru, ноль дрейфа)
  assert(file + ' hreflang x-default → RU', grab(/hreflang="x-default"\s+href="([^"]+)"/) === pairRu);
  assert(file + ' og:locale:alternate', grab(/property="og:locale:alternate"\s+content="([^"]+)"/) === (p.lang === 'en' ? 'ru_RU' : 'en_US'));
```

После HTML-цикла — шрифт-пары (кросс-копия головы молча теряет Caveat):
```js
const fontOf = f => (fs.readFileSync(path.join(ROOT, f), 'utf8').match(/fonts\.googleapis\.com\/css2[^"]*/) || [''])[0];
Object.entries(PAGES).filter(([, p]) => p.lang === 'en').forEach(([file, p]) => {
  const [twin] = Object.entries(PAGES).find(([, q]) => q.page === p.page && q.lang === 'ru');
  assert(file + ' шрифты === ' + twin, fontOf(file) === fontOf(twin), 'головы копировались кросс?');
});
```

- [ ] **Step 8: test.js — unit getLang + регрессия localStorage + prompt_lines parity**

После vm-инициализации sandbox (за строкой ~393), перед nav-блоком:
```js
// getLang — чистая функция pathname. Прямой заход на GH Pages даёт и /en/, и /en/index.html.
{
  const cases = [
    ['/', 'ru'], ['/index.html', 'ru'], ['/life/', 'ru'], ['/life/index.html', 'ru'],
    ['/en/', 'en'], ['/en', 'en'], ['/en/index.html', 'en'], ['/en/life/', 'en'],
    ['/en/life/index.html', 'en'], ['/energy/', 'ru'],
  ];
  cases.forEach(([pn, want]) => {
    sandbox.location = { pathname: pn };
    assert('getLang(' + pn + ') === ' + want, vm.runInContext('getLang()', sandbox) === want);
  });
  // Регрессия: localStorage мёртв. Остаточное чтение дало бы EN-тело на RU-URL
  // под RU-canonical — ровно тот дубль в индексе, ради которого затевалась фича.
  sandbox.localStorage = { getItem: () => 'en' };
  sandbox.location = { pathname: '/' };
  assert('getLang игнорирует localStorage', vm.runInContext('getLang()', sandbox) === 'ru');
  delete sandbox.localStorage;
}
```

Замена asserts prompt_lines (:48-49) на оба языка (отсутствие ключа в EN = краш страницы):
```js
['ru', 'en'].forEach(tag => {
  const pl = CONTENT[tag].hero.prompt_lines;
  assert(tag + ' hero.prompt_lines.cv', Array.isArray(pl.cv) && pl.cv.length >= 2);
  assert(tag + ' hero.prompt_lines.personal', Array.isArray(pl.personal) && pl.personal.length >= 2);
});
```

- [ ] **Step 9: test.js — nav-тест: 4 состояния из таблицы** (замена блока tabsOf/asserts из Task 1 Step 4)

```js
// renderNav — единственное место, где живут порядок вкладок, подсветка is-on и сетка URL.
// Гоняем все 4 состояния (страница × язык) прямо из PAGES: вкладки и бренд остаются
// в своём языке, href языкового переключателя === altOf (та же истина, что hreflang).
{
  Object.values(PAGES).forEach(p => {
    vm.runInContext('_lang = ' + JSON.stringify(p.lang), sandbox);
    const el = {};
    sandbox.renderNav(el, CONTENT[p.lang], p.page);
    const html = el.innerHTML;
    const seg = re => (html.match(re) || [])[1] || '';

    const nav = seg(/<div class="seg" role="navigation">([\s\S]*?)<\/div>/);
    const tabs = [...nav.matchAll(/<a href="([^"]*)" class="seg-btn([^"]*)"[^>]*>([^<]*)<\/a>/g)]
      .map(m => ({ href: m[1], on: m[2].includes('is-on'), text: m[3] }));

    const grp = seg(/<div class="seg" role="group"[^>]*>([\s\S]*?)<\/div>/);
    const langs = Object.fromEntries(
      [...grp.matchAll(/<a href="([^"]*)" hreflang="(ru|en)"/g)].map(m => [m[2], m[1]]));

    const cvURL = p.lang === 'en' ? '/en/' : '/';
    const lifeURL = p.lang === 'en' ? '/en/life/' : '/life/';
    assert('nav@' + p.url + ': работа первая, жизнь вторая, свой язык',
      tabs.length === 2 && tabs[0].href === cvURL && tabs[1].href === lifeURL,
      tabs.map(t => t.href).join(' | '));
    assert('nav@' + p.url + ': первая вкладка — это CV', tabs[0].text === CONTENT[p.lang].nav.cv, tabs[0].text);
    assert('nav@' + p.url + ': подсветка верна',
      tabs[0].on === (p.page === 'cv') && tabs[1].on === (p.page === 'personal'));
    assert('nav@' + p.url + ': lang self === свой URL', langs[p.lang] === p.url,
      langs[p.lang]);
    assert('nav@' + p.url + ': lang alt === altOf', langs[p.lang === 'ru' ? 'en' : 'ru'] === altOf(p.url),
      langs[p.lang === 'ru' ? 'en' : 'ru']);
    assert('nav@' + p.url + ': бренд в своём языке',
      html.includes('class="tb-brand" href="' + cvURL + '"'));
  });
  vm.runInContext('_lang = "ru"', sandbox);
}
```

- [ ] **Step 10: CLAUDE.md + README — минимальные правки**

CLAUDE.md, правка пункта `_lang` в «Код»:
```
- **`_lang` — глобальное состояние языка, выводится из URL.** `getLang()` парсит `location.pathname` (`/en/*` → en), localStorage не участвует. Не прокидывать lang параметром через всю цепочку, читать `_lang`. Внутренние ссылки строить через `pageURL(page, lang)`.
```
CLAUDE.md, «Структура» — две строки после `life/index.html`:
```
en/index.html           — EN CV (URL: /en/)
en/life/index.html      — EN личная (URL: /en/life/)
```

README.md, после «Editing content»:
```
## English version

`/en/` and `/en/life/` are separate indexable URLs (reciprocal hreflang pairs).
Same content.js — the `en` branch renders there; language is derived from the URL,
there is no client-side language state.
```

- [ ] **Step 11: прогнать тесты**

Run: `SKIP_PDF_GEN=1 node test.js`
Expected: `Result: N passed, 0 failed`.

- [ ] **Step 12: браузерная проверка (до коммита — обновить ?v= вручную)**

```bash
cd /Users/artfaal/PROJECTS/PERSONAL_PAGE/.claude/worktrees/en-i18n
for f in src/styles/base.css src/styles/layout.css src/styles/components.css \
         src/js/utils.js src/js/content.js src/js/icons.js src/js/shared.js \
         src/js/page-personal.js src/js/page-cv.js; do
  hash=$(md5 -q "$f" | cut -c1-8); base=$(basename "$f")
  for html in $(git ls-files '*index.html' '404.html') en/index.html en/life/index.html; do
    sed -i '' "s|${base}\(\?v=[a-f0-9]*\)\{0,1\}\"|${base}?v=${hash}\"|g" "$html"
  done
done
python3 -m http.server 8080
```

Чек-лист (Playwright/браузер):
- `/en/` → EN CV, терминал `cat ~/cv.md`, nav `job|life`, подсветка job + ru|en с подсветкой en;
- `/en/life/` → EN личная, Network-таб: все `src/`-запросы 200 (глубина `../../`);
- вкладки внутри EN ведут на `/en/` и `/en/life/`; бренд → `/en/`;
- переключатель: `/en/` → ru → `/`; `/life/` → en → `/en/life/`;
- PDF-кнопка на EN отдаёт `Solovev_Maksim_CV_en.pdf`;
- `/`, `/life/` — рендер и переключение не сломаны;
- view-source всех 4: canonical/og:url/hreflang различны и верны.

- [ ] **Step 13: СТОП — показать дифф Максу, ждать явного ОК на коммит**

```bash
git add en/ index.html life/index.html src/js/shared.js sitemap.xml test.js CLAUDE.md README.md
git commit -m "feat: английская версия на /en/ — индексируемая

Добавлены en/index.html и en/life/index.html — статические EN-головы
(canonical, og, взаимный hreflang ru/en/x-default), тело рендерит JS из
CONTENT.en. Язык теперь выводится из URL: getLang() парсит pathname,
setLang и localStorage выпилены, переключатель и вкладки nav — ссылки
из единой сетки pageURL(page, lang); внутри EN навигация остаётся в EN.
Sitemap расширен до четырёх URL. Тесты: hreflang-пары и og-статика всех
четырёх голов из таблицы PAGES, unit getLang, регрессия «localStorage
мёртв», parity prompt_lines, nav в четырёх состояниях."
```

- [ ] **Step 14: doc-keeper — аудит доки после мержа фичи** (после коммита, по глобальному правилу)

---

## Self-Review

- **Spec coverage:** 4.1-4.2 → Task 2 Steps 3-5; 4.3 → Steps 1-2; 4.4 → Step 6; 4.5 → Task 1 + Task 2 Steps 7-9; 4.6 → Step 10; §5 два коммита → Task 1/Task 2; §9 проверка → Steps 11-12. Гэпов нет.
- **Placeholders:** нет.
- **Type consistency:** `PAGES {url, lang, page}` и `altOf` едины в обеих задачах; `pageURL(page, lang)` — сигнатура совпадает в shared.js, nav-тесте и CLAUDE.md.
